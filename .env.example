PUBLIC_WHATSAPP_NUMBER=
PUBLIC_CONTACT_EMAIL=
PUBLIC_INSTAGRAM_URL=
PUBLIC_SITE_URL=

# Correo del formulario (Resend) — server-only, NUNCA con prefijo PUBLIC_
RESEND_API_KEY=
CONTACT_EMAIL_FROM=
CONTACT_EMAIL_TO=

# Módulo admin — base de datos y auth (server-only, NUNCA con prefijo PUBLIC_)
DATABASE_URL=                     # Neon Postgres (cadena pooled)
BETTER_AUTH_SECRET=               # secreto largo aleatorio (openssl rand -base64 32)
BETTER_AUTH_URL=                  # ej. https://chuterfc.com (local: http://localhost:4321)

# Seed del admin raíz (Camilo) — solo para scripts/seed-admin.mjs
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
SEED_ADMIN_NAME=
